services:
  mysql:
    image: mysql:8.0
    container_name: "popworld-mysql"
    ports:
      - "3306:3306"

    environment:
      - MYSQL_ROOT_PASSWORD=admin123
      - MYSQL_DATABASE=popworld_db

    volumes:
      - mysql_data:/var/lib/mysql

    restart: always

volumes:
  mysql_data:

